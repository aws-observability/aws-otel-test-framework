syntax = "proto3";

package opentelemetry.proto.collector.metrics.v1;

option go_package = "github.com/aws-observability/aws-otel-test-framework/mockedservers/grpcmetrics/proto";

// The Metric Handler service definition.
service MetricsService {
    // Export data
    rpc Export (Request) returns (ExportMetricsResponse) {}
}

message Request {
    string name = 1;
}

message ExportMetricsResponse {
    string message = 1;
}
